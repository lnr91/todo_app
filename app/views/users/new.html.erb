<%= provide(:title, 'Sign up') %>

<div class="row">

  <div class="span6 offset1">
    <h2>Sign up</h2>
    <%= form_for(@user) do |f| %>
        <%= render 'fields', f: f %>
        <%= f.submit "Sign up", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>